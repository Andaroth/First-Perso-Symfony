<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Platform Advert View</title>
</head>
<body>
  <h1>Chez {{author}}</h1>
  
  {% if tag != null %}
  <a href="{{url}}/{{id+1}}">[{{tag}}] Link to here +1 : {{id+1}}</a>
  {% else %}
  <a href="{{url}}/{{id+1}}">Link to here +1 : {{id+1}}</a>
  {% endif %}
  
  {% if ( (first != null) and (second != null) ) %}
  <table>
    <thead>
      <tr>
        <td>First</td>
        <td>Second</td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>{{first}}</td>
        <td>{{second}}</td>
      </tr>
    </tbody>
  </table>
  {% else %}
  <form action="./" method="post">
    <label for="first">First :</label>
    <input name="first" type="text">
    <label for="second">Second :</label>
    <input name="second" type="text">
    <input type="submit">
  </form>
  {% endif %}
</body>
</html>